# Lead Conversion Prediction
Interactive Streamlit portfolio demo for scoring marketing leads, prioritizing follow-up, and explaining conversion-model performance. Uses synthetic data only.

## Run
`pip install -r requirements.txt && streamlit run app.py`
